---
id: REQ-CROSS-DEVICE-TAB-SYNC-001
status: draft
---

# 两个窗口的新标签数量同步

## 背景

用户同时在两个浏览器窗口使用同一工作区，需要在一个窗口新建标签后，在另一个窗口看到相同的标签数量。

## 需求目标

在任一窗口新建一个标签后，另一个窗口自动同步新增标签，两个窗口的标签数量保持一致。

## 范围

两个已登录窗口打开同一工作区，先确认标签数量一致，再在其中一个窗口新建一个标签，检查两端数量均增加一个且一致。

## 非目标

关闭标签的同步、输入焦点和终端显示及连接稳定性由各自场景验收。
