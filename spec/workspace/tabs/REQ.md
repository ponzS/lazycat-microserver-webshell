---
id: REQ-WORKSPACE-TABS-001
status: draft
---

# 工作区标签创建与命名保持可用

## 背景

授权用户通过浏览器使用 WebShell。用户已打开包含终端的工作区时，相关交互的回归会影响用户继续使用终端。

## 需求目标

新标签能打开终端，修改后的名称在刷新后仍保留。

## 范围

用户创建标签、修改标签名称并刷新页面，并保护本功能对应的外部结果。

## 非目标

窗格恢复和断网恢复不归入标签命名合同。
