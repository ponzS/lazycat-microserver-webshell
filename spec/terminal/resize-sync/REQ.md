---
id: REQ-MULTI-DEVICE-RESIZE-SYNC-001
status: draft
---

# 两个设备交替使用终端时尺寸与输入恢复正常

## 背景

授权用户通过浏览器使用 WebShell。两个授权浏览器打开同一终端时，相关交互的回归会影响用户继续使用终端。

## 需求目标

终端尺寸适应最后使用的窗口，连接恢复后输入按顺序执行且不重复。

## 范围

用户交替调整窗口并在一次连接恢复期间输入命令，并保护本功能对应的外部结果。

## 非目标

移动视口模拟不代表物理移动设备。
