---
id: REQ-TERMINAL-INTERACTION-JITTER-001
status: draft
---

# 输入和选择文本时终端画面保持稳定

## 背景

授权用户通过浏览器使用 WebShell。桌面终端已有稳定内容时，相关交互的回归会影响用户继续使用终端。

## 需求目标

命令产生对应输出，输入和选择期间画面不跳动或短暂隐藏。

## 范围

用户点击终端、输入命令并拖动选择文本，并保护本功能对应的外部结果。

## 非目标

移动端原生文本选择另行验收。
