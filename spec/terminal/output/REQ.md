---
id: REQ-TERMINAL-OUTPUT-001
status: draft
---

# 连续大量输出期间终端内容与交互保持稳定

## 背景

授权用户通过浏览器使用 WebShell。用户已打开持续可交互的终端时，相关交互的回归会影响用户继续使用终端。

## 需求目标

输出保持完整可见，操作后终端仍可继续交互。

## 范围

终端持续输出大量内容，用户同时输入并调整窗口或切换标签，并保护本功能对应的外部结果。

## 非目标

客户端历史回放由对应功能验收。
