---
id: REQ-TERMINAL-INPUT-001
status: draft
---

# 终端键盘输入与控制操作保持可用

## 背景

授权用户通过浏览器使用 WebShell。用户已打开可交互终端时，相关交互的回归会影响用户继续使用终端。

## 需求目标

输入得到对应输出，控制操作及大段文本输入后终端仍可继续使用。

## 范围

用户连续执行命令、取消运行中的命令并输入大段文本，并保护本功能对应的外部结果。

## 非目标

系统输入法的设备操作另行验收。
