---
id: REQ-CLIENT-TAB-REPLAY-RECOVERY-001
status: draft
---

# 客户端终端多标签历史恢复后仍可交互

## 背景

授权用户通过浏览器使用 WebShell。授权且在线的客户端终端拥有多个含不同历史长度的标签时，相关交互的回归会影响用户继续使用终端。

## 需求目标

每个标签恢复自己的终端内容并响应后续输入。

## 范围

用户刷新、切换标签并重连后继续输入，并保护本功能对应的外部结果。

## 非目标

普通容器不能替代客户端终端验收。
