---
id: REQ-TERMINAL-INPUT-LOCK-LIFECYCLE-001
status: draft
---

# 旧客户端的输入锁请求不阻塞其他连接

## 背景

授权用户通过浏览器使用 WebShell。两个授权页面连接同一终端，其中一个使用旧客户端输入锁行为时，相关交互的回归会影响用户继续使用终端。

## 需求目标

另一个页面仍得到对应输出并可继续操作同一终端。

## 范围

旧客户端发出锁定请求后另一个页面输入命令，并保护本功能对应的外部结果。

## 非目标

内部协议处理方式不属于用户合同。
