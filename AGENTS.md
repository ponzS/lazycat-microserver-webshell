# AGENTS.md

## 项目级约束

- 始终使用简体中文回应。
- 开始任务前必须先阅读 [FIX_HISTORY.md](docs/FIX_HISTORY.md)，确认相关模块的架构边界、历史问题和现有 guard。
- 每次修复 Bug 时必须同步更新 `docs/FIX_HISTORY.md`，写清现象、根因、实施方案、回归 guard 和验证结果。
- 修复不能只依赖内存记忆；优先增加自动化测试，无法自动化时必须记录可重复的最小验证步骤。
- Node 行为测试统一放在仓库根目录的 `tests/` 文件夹中，文件名使用 `*_test.mjs`；根目录不得新增或保留这类测试文件。`tests-auto/` 仅用于真实浏览器/设备回归，不与 Node 单元测试混放。

## 前端模块化与入口边界

- 开始新增或整理前端代码前，必须阅读 [FRONTEND_MODULE_MAP.md](docs/FRONTEND_MODULE_MAP.md)；新增模块、改变状态所有权或调整依赖方向时，必须在同一次变更中更新该文档。
- `runtime/static/main.js` 只能作为前端启动入口。
  - 只允许导入应用根模块并调用单一启动入口。
  - 不得在其中实现业务功能、维护业务状态、查询或缓存成组 DOM、注册具体交互事件、创建定时器、发起 API/WebSocket 请求、实现状态机，或承担跨模块逻辑编排和生命周期管理。
  - 全局启动、挂载、恢复、在线/离线、页面显隐和销毁等编排必须由独立的应用生命周期文件维护；`main.js` 不得成为该逻辑的代理存放处。
  - 整理现有代码期间，`main.js` 只允许减少实现代码，或调整为导入和调用模块入口；不得继续向其中追加功能实现。
- `runtime/static/global-runtime.js` 是应用根目录下唯一的全局运行时 owner。
  - 全局状态、全局生命周期、启动/恢复/挂起/销毁顺序、页面级资源和跨模块初始化编排统一维护在此文件中。
  - `global-runtime.js` 可以创建 feature controller、保存它们的实例引用并通过公开 API 串联流程，但不得复制各业务模块的内部状态，也不得借“全局运行时”名义继续实现新的业务算法。
  - `app/app_lifecycle.js`、各 feature 的 `*_lifecycle.js` 只负责可复用的局部资源注册/清理工具；它们不能取代全局 runtime 成为根应用初始化或销毁顺序的 owner。
  - 所有全局状态字段必须在 `global-runtime.js` 有清晰声明、唯一写入边界和 dispose 规则；跨模块共享必须通过显式 getter、命令、事件或只读快照完成。
  - 迁移已有逻辑时，先把业务实现迁入对应模块，再由 `global-runtime.js` 保留最小的依赖接线和生命周期调用；不得仅以复制代码或改名文件制造“模块化”。
- 新增功能必须先确定模块归属。属于现有模块职责的能力放入该模块文件夹；新的独立责任域必须在 `runtime/static/` 下创建独立文件夹维护，不得重新堆入静态资源根目录或 `main.js`。
- 每个前端模块文件夹根目录必须包含 `README.md`，至少写明：
  - 模块职责和明确不负责的范围。
  - 对外入口、公开 API、事件或数据契约。
  - 状态所有权以及允许修改这些状态的唯一控制器。
  - 模块启动、停止、恢复和销毁流程，以及 timer、observer、listener、socket 等资源的清理责任。
  - 文件清单和每个文件的用途。
  - 依赖方向、相关测试、历史 guard 和最小回归步骤。
- 模块内部的逻辑编排必须由独立的 `controller.js`、`orchestrator.js` 或等价文件维护；存在浏览器或资源生命周期时，必须由独立的 `lifecycle.js` 或等价文件维护。纯算法、协议解析、DOM 适配和持久化实现不得同时承担模块编排职责。
- 应用级共享状态必须有唯一 owner，并通过显式依赖、方法、事件或只读快照交互；不得通过可变全局对象、隐式 `window` 字段、跨模块直接修改对象字段或循环依赖共享状态。
- 模块应提供单一公开入口，外部不得深度导入模块内部文件。模块必须提供幂等、可重复调用的清理入口；注册的事件、timer、observer、WebSocket 包装和异步任务都必须能按 generation 或 dispose 状态拒绝迟到回调。
- 重构现有前端代码时按模块边界逐步迁移，每次只迁移一个可验证的责任域，并同步迁移或增加自动化 guard。移动静态模块时必须同时检查相对 import、Service Worker 预缓存、版本化资源、LPK 打包内容和现有 Go/Node 静态契约测试。
- `shared` 或 `utils` 只能保存无业务状态的通用纯函数，禁止把无法归属的业务逻辑放入通用目录。终端 session、连接、历史、渲染、resize、输入和缓存状态必须各自归属明确模块，不能形成新的共享大对象。
- 本项目禁止引入 `tmux`。
  - 不得把 `tmux` 添加为运行依赖、构建依赖、镜像/打包依赖或脚本依赖。
  - 不得在 Go、Shell、配置文件、构建文件或运行时逻辑中调用 `tmux`。
  - 不得基于 `tmux` 实现会话、pane、tab、复用、保活或终端管理能力。
- 本项目禁止引入 `xterm.js`。
  - 不得添加 `xterm`、`@xterm/*`、`xterm-addon-*` 等 npm 依赖。
  - 不得复制、内嵌、改名或 vendoring `xterm.js` 及其插件源码。
  - 不得基于 `xterm.js` API 设计或实现前端终端功能。
- 如果未来新增 WebShell、终端渲染、会话管理、tab/pane、保活或复用能力，必须遵守以上限制；如果某个方案依赖 `tmux` 或 `xterm.js`，需要先改用不依赖它们的方案，而不是绕过本规则。
- 参考外部示例代码时，如果示例包含 `tmux` 或 `xterm.js`，只能作为概念参考，不得把相关代码、依赖或调用路径带入本项目。
- 任何情况下和任何逻辑编排都必须禁止渲染历史回放过程，任何改动都应该优先考虑这个问题，避免问题回归
- 新增模块必须遵守上述目录、文档、状态所有权和生命周期边界，禁止高耦合或以拆文件代替真正的模块解耦。
