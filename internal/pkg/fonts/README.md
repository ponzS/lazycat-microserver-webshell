# 字体与终端设置

维护字体名称/资源解析、设置存储及终端配置默认值。`name.go` 处理字体名称，`store.go` 维护存储和校验；Provider 使用完整设置能力，Core 仅使用历史行数等配置定义。

本阶段只补模块说明，不改变现有设置键、数据格式、默认值或文件位置。不得添加 PTY/平台操作或依赖 Provider。

根目录执行 `go build ./internal/pkg/fonts`；手测字体选择、上传/删除和历史行数设置，并检查刷新后配置保留。
