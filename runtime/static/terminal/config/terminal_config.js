// Immutable terminal-domain configuration. Runtime state belongs to the
// owning controller; this module only exposes shared defaults and timings.
export const TERMINAL_STORAGE_PREFIX = "webshell";

export const TERMINAL_RUNTIME_CONFIG = Object.freeze({
  touchShortcutMoveThresholdPx: 8,
  touchSelectionMoveThresholdPx: 7,
  touchSelectionLongPressDelayMs: 450,
  mobileSelectionAutoScrollEdgePx: 34,
  mobileSelectionAutoScrollIntervalMs: 50,
  mobileSelectionAutoScrollMaxLines: 4,
  mobileKeyboardDoubleTapDelayMs: 320,
  mobileKeyboardFocusAllowWindowMs: 600,
  mobileKeyboardFocusPrompt: "双击屏幕开启键盘输入",
  desktopSelectionCopyMoveThresholdPx: 4,
  terminalSizeReassertIntervalMs: 250,
  terminalSizeClaimIntervalMs: 250,
  terminalCursorBlinkHoldMs: 700,
  terminalWebSocketPingIntervalMs: 10 * 1000,
  terminalUnifiedHealthCheckIntervalMs: 4 * 1000,
  terminalUnifiedPongTimeoutMs: 12 * 1000,
  terminalUnifiedTransitionTimeoutMs: 12 * 1000,
  terminalWebSocketConnectTimeoutMs: 12 * 1000,
  terminalClientDirectWebSocketCapacity: 3,
  terminalConnectionInteractionPriorityMs: 1200,
  terminalUnifiedPaneRetryBaseDelayMs: 500,
  terminalUnifiedPaneRetryMaxDelayMs: 10 * 1000,
  terminalWebSocketHealthTimeoutMs: 25 * 1000,
  terminalResumeProbeTimeoutMs: 1500,
  terminalUserRecoveryThrottleMs: 1500,
  terminalAttachReadyTimeoutMs: 8 * 1000,
  terminalAgentPrepareTimeoutMs: 45 * 1000,
  terminalReconnectBaseDelayMs: 500,
  terminalReconnectMaxDelayMs: 10 * 1000,
  terminalReconnectJitterRatio: 0.2,
  terminalResizeThrottleMs: 80,
  terminalResizeSettleMs: 120,
  terminalResizeOutputQuietMs: 120,
  terminalResizeOutputMaxHoldMs: 800,
  terminalReplayFailureLimit: 3,
  terminalReplayCheckpointDelayMs: 48,
  terminalHistoryCacheFlushBytes: 256 * 1024,
  terminalCacheV2FlushBytes: 1 * 1024 * 1024,
  terminalHistoryCacheFlushDelayMs: 50,
  terminalCacheV2FlushDelayMs: 1000,
  terminalCacheV2PreviewDelayMs: 3000,
  terminalCacheV2PreviewRefreshMs: 2000,
  terminalHistoryCacheOrphanTTL: 30 * 1000,
  terminalCacheV2TouchIntervalMs: 5 * 60 * 1000,
  terminalCacheV2ManifestTimeoutMs: 1500,
  terminalCacheV2PreviewTimeoutMs: 3000,
  terminalCacheV2ReplayTimeoutMs: 2 * 1000,
  terminalCacheV2CommitTimeoutMs: 3000,
  terminalCacheV2CompactionMinChunks: 2,
  terminalCacheV2CompactionTargetBytes: 1 * 1024 * 1024,
  averageTerminalHistoryBytesPerLine: 350,
  activityPollIntervalMs: 4000,
});
